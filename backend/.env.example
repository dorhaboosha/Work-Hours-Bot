# Required — set these as environment variables in your deployment (e.g. Render)
DATABASE_URL=postgresql://user:password@host:5432/dbname
TELEGRAM_BOT_TOKEN=your-telegram-bot-token-here
NODE_ENV=production

# Optional — Render injects PORT automatically; only needed for local dev
PORT=3000
