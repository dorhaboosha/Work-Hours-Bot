{
  "name": "shared",
  "version": "0.0.1",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "test": "node --require ts-node/register --test src/**/*.test.ts"
  },
  "devDependencies": {
    "typescript": "^5.5.2"
  }
}
